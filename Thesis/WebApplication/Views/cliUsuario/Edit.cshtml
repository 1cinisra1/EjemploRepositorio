@model WebApplication.Models.cli_usuario

@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>

@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()
    
    <div class="form-horizontal">
        <h4>cli_usuario</h4>
        <hr />
        @Html.ValidationSummary(true)
        @Html.HiddenFor(model => model.idCli_Usuario)

        @Html.HiddenFor(model => model.Cli_Departamento_idCli_Departamento)

        @Html.HiddenFor(model => model.Cli_Equipo_idCli_Equipo)

        @Html.HiddenFor(model => model.Cli_Empresa_idCli_Empresa)

        <div class="form-group">
            @Html.LabelFor(model => model.Cli_nombre, new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.EditorFor(model => model.Cli_nombre)
                @Html.ValidationMessageFor(model => model.Cli_nombre)
            </div>
        </div>

        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Save" class="btn btn-default" />
            </div>
        </div>
    </div>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>
