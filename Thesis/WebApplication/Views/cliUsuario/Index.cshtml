@model IEnumerable<WebApplication.Models.cli_usuario>

@{
    ViewBag.Title = "Detalle de Clientes";
}

@section Menu {
    @Scripts.Render("~/Scripts/Tables/Data/menu")
}

@Styles.Render("~/AdminLTE/plugins/datatables/css")

<p>
    @Html.ActionLink("Create New", "Create")
</p>


<div class="row">
    <div class="col-xs-12">
        <div class="box">
            <div class="box-header">
                <h3 class="box-title">Datos de Clientes</h3>
            </div>
            <div class="box-body">
                <table id="example1" class="table table-bordered table-striped">
                    <thead>
                        <tr>
                            <th>Nombre</th>
                            <th>Departamento</th>
                            <th>Empresa</th>
                            <th> Equipo</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in Model)
                        {
                            <tr>
                                <td>
                                    @Html.DisplayFor(modelItem => item.Cli_nombre)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.cli_departamento1.Cli_Descripcion)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.cli_empresa.Cli_Nombre)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.Cli_Equipo_idCli_Equipo)
                                </td>
                                @*<td>
                                        @Html.ActionLink("Edit", "Edit", new { id = item.idCli_Usuario, id1 = item.Cli_Empresa_idCli_Empresa, id2 = item.Cli_Departamento_idCli_Departamento }) |
                                        @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
                                        @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
                                    </td>*@
                            </tr>
                        }
                    </tbody>
                    <tfoot>
                        <tr>
                            <th>Nombre</th>
                            <th>Departamento</th>
                            <th>Empresa</th>
                            <th> Equipo</th>
                        </tr>
                    </tfoot>
                </table>
            </div>
        </div>
    </div>
</div>
@section Scripts {
    @Scripts.Render("~/AdminLTE/plugins/datatables/js")
    @Scripts.Render("~/Scripts/Tables/Data")
}