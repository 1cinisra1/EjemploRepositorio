@model WebApplication.Models.cli_departamento

@{
    ViewBag.Title = "Edit";
}



<div class="register-box">
    <div class="register-box-body">
        <p class="login-box-msg">Registrar una nueva Empresa</p>

        @using (Html.BeginForm())
        {
            @Html.AntiForgeryToken()

            <div class="form-horizontal">
                <h4>Editar Informacion</h4>
                <hr />
                @Html.ValidationSummary(true)
                @Html.HiddenFor(model => model.idCli_Departamento)

                <div class="form-group">
                    <label for="inputEmail3" class="col-sm-2 control-label">Descripcion</label>
                    <div class="col-md-10">
                        @Html.EditorFor(model => model.Cli_Descripcion)
                        @Html.ValidationMessageFor(model => model.Cli_Descripcion)
                    </div>
                </div>

                <div class="form-group">
                    <div class="col-md-offset-2 col-md-10">
                        <input type="submit" value="Guardar" class="btn btn-default" />
                    </div>
                </div>
            </div>
        }

        <div>
            @Html.ActionLink("Back to List", "Index")
        </div>
    </div>
</div>

